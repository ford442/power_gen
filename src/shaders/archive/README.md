# Archived WGSL

Legacy or experimental modules kept for reference. **Excluded** from `npm run check:wgsl` / naga.

To validate an archived file explicitly, copy it out or add a one-off entry in `scripts/extract-wgsl.mjs`.
